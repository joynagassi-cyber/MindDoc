# SearxNG settings — format JSON + Wolfram Alpha. Réf : c02 web search engine V2
use_default_settings: true
server:
  bind_address: "0.0.0.0"
  port: 8080
  secret_key: "scy-searxng-dev-key-replace-me"
search:
  formats:
    - html
    - json
outgoing:
  request_timeout: 3.0
