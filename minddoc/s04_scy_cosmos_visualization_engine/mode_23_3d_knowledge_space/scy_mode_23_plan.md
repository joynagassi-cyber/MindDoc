# 🛠️ SCY-MODE-23 — PLAN (PLAN)
**ID** : S04_COSMOS_MODE_23_PLAN · **Moteur** : three.js (D-RENDER-009)
## Flux : [coordonnées 3D 3 axes] → GPU check (WebGL2+GPU) → non → masquer M23 + fallback Mode 2 ; oui → lazy three.js → Mode23_3D.tsx (sphères ∝ domaine/importance, cylindres, axes 3D @60FPS) → clic → fly-to + Knowledge Card 3D.
## Dépendances : `three` + OrbitControls lazy. Fichiers : `frontend_react/src/cosmos/modes/Mode23_3DKnowledge.tsx`.
