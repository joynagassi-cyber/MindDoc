{
  "name": "@scy-forge/backend-ts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SCY Forge — orchestration agents ASCENT (TypeScript + Mastra)",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "echo 'TODO: vitest' && exit 0",
    "lint": "echo 'TODO: eslint' && exit 0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mastra/core": "^1.0.0",
    "@nangohq/node": "^0.40.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
